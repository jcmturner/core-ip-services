<% node['forward-zone'].each do |zone, forwardtoips| -%>
zone "<%= zone %>" {
  type forward;
  forward only;
  forwarders { 
  <% forwardtoips.each do |forwardtoip| -%>
    <% unless forwardtoip.empty? -%>
    <%= forwardtoip %>;
    <% end %>
  <% end %>
  };
};
<% end %>

zone "coreip.intranet" {
	type master;
	file "/var/named/data/db.coreip.intranet";
};

